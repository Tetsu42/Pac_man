# Pac_man
reproduire le jeux pacman avec python

# Utilisation de librairie pour les graphiques, deplacement, son , collision ,...

installer pygame avec pip install pygame.

# visualisation dans le navigateur
    pip install pygbag --user --upgrade
    python3 -m pygbag dossier_du_projet
